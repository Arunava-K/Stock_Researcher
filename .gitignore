.venv/
__pycache__/
*.pyc

# Local-only state — for cloud Routine, state lives in the Notion "Routine State" page.
# Don't commit local copies; they'll go stale.
data/
briefs/

# Claude Code local settings — user-specific, not for the repo
.claude/settings.local.json

.DS_Store
